class Solution:
    def reverseWords(self, s: str) -> str:

        list = s.split()
        list.reverse()
        # print(list)
        res = ""

        for words in list:
            res+=words + " "

        return res.strip()
