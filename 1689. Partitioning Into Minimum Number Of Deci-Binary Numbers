class Solution {
public:
    int minPartitions(string n) {
        int res = n[0]-'0';
        for(int j = 1; j<n.size();j++){
            int num = n[j]-'0';
            res = max(num,res);
        }
        return res;
    }
        
        
    
};
